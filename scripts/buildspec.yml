version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 16
    commands:
      - echo "Installing dependencies"
      - yum install
       build:
    commands:
      - echo "Build phase started"
      - systemctl start nodejs
   

artifacts:
  files:
    - "**/*"
